{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "npm run build -w src",
  "outputDirectory": "src/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
