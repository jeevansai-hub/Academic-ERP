{
  "name": "ecap-viit",
  "version": "1.0.0",
  "description": "ECAP — VIIT Student Performance Portal",
  "scripts": {
    "dev:frontend": "npm run dev -w src",
    "dev:functions": "npm run serve -w functions",
    "build": "npm run build -w src && npm run build -w functions"
  },
  "workspaces": [
    "src",
    "functions"
  ],
  "dependencies": {
    "firebase": "^10.4.0"
  }
}
